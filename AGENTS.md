# 知识库维护规范

本仓库采用 LLM Wiki 模式：用户负责提供资料、提出问题和确定分析方向；Agent 负责整理、交叉引用和持续维护知识库。

## 目录职责

- `raw/`：原始资料，是事实来源。只读，不修改、重写、移动或删除其中的文件。
- `raw/assets/`：原始资料引用的图片和附件，同样只读。
- `wiki/`：Agent 维护的 Markdown 知识库。用户主要阅读，Agent 负责创建和更新。
- `wiki/index.md`：内容索引，按主题列出页面链接和一句话摘要。
- `wiki/log.md`：按时间追加的操作记录，不改写既有记录。

## 基本原则

1. 事实与结论必须能追溯到 `raw/` 中的来源；无法确认的内容明确标记为推测或待核实。
2. 新资料应整合进现有页面，避免仅生成孤立摘要或重复页面。
3. 发现不同来源相互矛盾时，保留各方说法、对应来源和时间信息，不擅自抹平冲突。
4. 使用 Obsidian 双向链接 `[[页面名]]` 连接相关实体、概念和主题。
5. 页面名称应清晰、稳定；除非确有必要，不批量改名或调整目录。
6. 默认进行最小范围修改，不顺带重构无关内容。

## 工作流程

### 摄入资料

1. 阅读指定的 `raw/` 来源，并确认其类型、日期和主题。
2. 提取关键事实、观点、证据、限制条件和潜在矛盾。
3. 创建或更新相关 `wiki/` 页面，并补充必要的双向链接。
4. 更新 `wiki/index.md` 中的页面条目和一句话摘要。
5. 在 `wiki/log.md` 追加一条 `ingest` 记录。

### 回答问题

1. 先查看 `wiki/index.md`，再读取相关页面和必要的原始来源。
2. 回答中区分来源事实、综合判断和不确定项，并引用相关 Wiki 页面或原始资料。
3. 只有当回答具有长期复用价值时，才将其整理为新页面或更新既有页面。
4. 若写入 Wiki，更新索引并在日志追加 `query` 记录。

### 健康检查

检查相互矛盾或过时的陈述、孤立页面、缺失链接、重复内容、未建立页面的重要概念，以及缺少来源支撑的结论。实际修改后，在日志追加 `lint` 记录。

## 页面约定

- Markdown 文件使用 UTF-8 编码。
- 每个页面围绕一个稳定主题，开头用简短段落说明主题和当前结论。
- 引用原始资料时使用相对链接，例如 `[[../raw/文件名]]`；必要时注明章节、页码或段落。
- 不为尚无实际内容的分类预先创建大量目录或占位页面。

## 日志格式

每条记录使用以下标题格式，日期采用本地时间：

```text
## [YYYY-MM-DD] ingest | 标题
## [YYYY-MM-DD] query | 问题或产出
## [YYYY-MM-DD] lint | 检查范围
```

标题下简要记录涉及的来源、修改页面和重要判断。

## 面试抽查 Skill

本项目提供 `interview-quiz` skill，用于只读抽查 `c++interview/` 与 `llm-algo-leetcode/` 中的 C++、Algorithm 知识，并在独立目录维护错题和复习计划。

- 使用 `$interview-quiz [cpp|algorithm|all]` 开始一轮面试抽查；未指定范围时使用 `all`，生成 10 道题，一次回答一道。
- 使用 `$interview-quiz review [cpp|algorithm|all]` 只复习指定范围内的到期错题。
- 派生数据写入 `interview-review/`，不得修改、移动或删除上述两个知识源。
- 默认遗忘曲线间隔为 1、3、7、14、30 天；用户明确指定节奏时，以用户设置为准。
- 错误或部分正确的反馈必须包含具体 C++ 代码、算法输入输出或反例，不能只给抽象解释。
- 反馈可加入适量程序员梗、拟人化或善意吐槽，采用“结论 + 轻松解释 + 示例”；不得嘲讽用户或牺牲技术准确性。
